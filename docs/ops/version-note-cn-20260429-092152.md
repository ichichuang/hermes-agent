# 版本说明（临时快照）

## 背景
- 时间：2026-04-29
- 场景：`main` 分支与 `origin/main` 严重分叉（本地 ahead 4，远端领先 1100+），导致 `git pull --ff-only` 失败。
- 目标：先完整保全本地代码与未提交修改，再执行分叉修复。

## 本次快照包含内容
- 本地 `main` 上的 4 个历史提交。
- 当前工作区所有已跟踪修改。
- 当前工作区所有未跟踪文件（含 `core/output/sanitizer.py`、`gateway/feishu_progress.py`、对应测试文件等）。

## 修复过程记录
1. 执行全量安全暂存（含未跟踪文件）。
2. 创建分叉前备份分支：`backup/main-diverged-pre-rebase-20260429-092152`。
3. 拉取远端后尝试 `rebase origin/main`。
4. 首个提交即出现多文件冲突，按预案立即 `rebase --abort`，避免在复杂冲突态下错误合并。
5. 在本快照分支恢复本地改动并提交，确保可回滚、可审计、可远端备份。

## 冲突摘要（用于后续人工处理）
- `gateway/run.py`
- `gateway/session.py`
- `hermes_cli/commands.py`

## 后续建议
- 以本快照分支为基线进行人工冲突整理。
- `main` 分支回到 `origin/main` 后，再通过 `cherry-pick`/小批量 PR 逐步回放本地改动。
